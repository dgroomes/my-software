# Don't bundle the Kotlin stdlib in the plugin distribution because the IntelliJ Platform
# already provides a compatible version at runtime.
# See https://plugins.jetbrains.com/docs/intellij/using-kotlin.html#kotlin-standard-library
kotlin.stdlib.default.dependency = false
